// forin loop

const newObj = {
    js: "javascript",
    cpp: "C++",
    rb: "ruby",
    swift: "swift by app"
}

for (const key in newObj) {
    // console.log(`${key} is the shortform of ${newObj[key]}`);   
}

const languages = ["java","python","cpp ","rust"]

for (const key in languages) {
    // console.log(languages[key]);
}

// const map = new Map()
// map.set("IND","INDIA")
// map.set("CHN","CHINA")
// map.set("UK","UNITED KINGDOM")

// for (const key in map) {
//     console.log(key);
// }

